import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles(() => ({
  small: {
    fontSize: '0.875rem',
  },
  icon: {
    alignSelf: 'center',
  },
  pricingLeagueHelper: {
    margin: 0,
    marginTop: 5,
    color: '#ffc107',
  },
}));

export default useStyles;
