import makeStyles from '@mui/styles/makeStyles';

const useStyles = makeStyles(() => ({
  small: {
    fontSize: '0.875rem',
  },
  icon: {
    alignSelf: 'center',
  },
}));

export default useStyles;
